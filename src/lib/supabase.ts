import { createClient } from "@supabase/supabase-js"; const supabaseUrl = import.meta.env.VITE_SUPABASE_URL?.trim() || "http://localhost:54321"; const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY?.trim() || "anon-key-placeholder"; export const supabase = createClient(supabaseUrl, supabaseAnonKey);